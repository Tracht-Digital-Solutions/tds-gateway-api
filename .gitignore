/vendor/
/.env
/.env.local
/.env.docker
/dist/
/logs/
# Auto-migration marker + single-flight lock (Support\MigrationRunner).
# Gitignored so it survives a git-pull deploy but is never committed.
/var/
# Generated by bin/gen-api-wiki.php — the live page is a build artifact
# (CI regenerates it into the bundle). API-WIKI.md stays tracked as the
# readable snapshot.
/wiki/
/.idea/
/.vscode/
/.phpunit.cache/
.phpunit.result.cache
/composer.phar
.DS_Store
*.log

# Windows OS files
Thumbs.db
Desktop.ini

# Editor backups
*.swp
*~
